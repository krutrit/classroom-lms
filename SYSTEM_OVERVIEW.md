# เอกสารภาพรวมระบบ LMS / กระดานคะแนนห้องเรียน

> จัดทำจากการตรวจสอบโค้ดแบบ Static Analysis ณ วันที่ 3 กันยายน 2026  
> ขอบเขตที่ตรวจ: `code.gs`, `index.html`, `teacher.html`, `script.html` และ `style.html`

## 1. สรุประบบ

ระบบนี้เป็นเว็บแอปสำหรับจัดการคะแนนและติดตามผลการเรียนในห้องเรียน พัฒนาด้วย Google Apps Script และใช้ Google Sheets เป็นฐานข้อมูลหลัก พร้อมใช้ Google Drive เก็บรูปนักเรียน

ระบบแบ่งออกเป็น 2 หน้าหลัก:

1. **หน้าสาธารณะ (`index.html`)** — แสดงกระดานคะแนน อันดับ ความก้าวหน้า รายละเอียดการส่งงาน เหรียญรางวัล และมินิเกม
2. **หน้าครู (`teacher.html`)** — จัดการนักเรียน งาน คะแนน ตัวชี้วัด รายงาน มินิเกม และการตั้งค่าระบบ

จุดเริ่มต้นของ Web App คือฟังก์ชัน `doGet(e)` ใน `code.gs`:

- URL ปกติเปิดหน้ากระดานคะแนน
- `?page=teacher` เปิดหน้าจัดการของครู
- หน้าแรกมี Script Cache อายุ 2 นาที เพื่อลดเวลา render
- หน้าเว็บอนุญาตให้ฝังใน iframe ผ่าน `XFrameOptionsMode.ALLOWALL`

## 2. เทคโนโลยีและบริการที่ใช้

| ส่วน | เทคโนโลยี |
|---|---|
| Backend | Google Apps Script (`code.gs`) |
| Frontend | HTML, CSS และ JavaScript แบบ Vanilla |
| ฐานข้อมูล | Google Sheets หลายชีต |
| ที่เก็บรูป | Google Drive |
| การสื่อสาร Client/Server | `google.script.run` |
| กราฟ | Chart.js 4.4.1 จาก CDN |
| Excel | `xlsx-js-style` 1.2.0 จาก CDN |
| ฟอนต์ | Google Fonts: Prompt และ Sarabun |
| PDF | สร้างหน้า HTML สำหรับพิมพ์/บันทึกเป็น PDF ผ่าน Browser |

ไม่มีไฟล์ `appsscript.json`, ระบบ package, automated test หรือ Git repository อยู่ในโฟลเดอร์ที่ตรวจพบ

## 3. โครงสร้างไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `code.gs` | Backend, อ่าน/เขียน Google Sheets, คำนวณคะแนน, อัปโหลดรูป, ตรวจรหัสครู, มินิเกม และเตรียมข้อมูลรายงาน |
| `index.html` | หน้ากระดานคะแนนสำหรับนักเรียน/ผู้ชมทั่วไป |
| `teacher.html` | หน้าจัดการทั้งหมดสำหรับครู รวม CSS และ JavaScript เฉพาะหน้านี้ |
| `style.html` | CSS กลางของหน้าสาธารณะและ component ที่ใช้ร่วมกัน |
| `script.html` | JavaScript กลาง เช่น Toast, ปิด Modal และแปลง URL รูปจาก Google Drive |

## 4. ความสามารถของหน้าสาธารณะ

### 4.1 กระดานคะแนน

- แสดงนักเรียนที่มีสถานะ `active`
- แยกดูตามห้องเรียนหรือดูทุกห้อง
- ค้นหานักเรียนจากชื่อ
- แสดงอันดับ Top 3 แบบ Podium
- แสดงการ์ดนักเรียน พร้อมรูป ชื่อ ห้อง คะแนนรวม อันดับ และ progress
- รองรับกรณีคะแนนเท่ากัน โดยพิจารณาตามลำดับ:
  1. คะแนนรวมมากกว่า
  2. จำนวนงานที่ส่งมากกว่า
  3. เวลาส่งงานล่าสุดเร็วกว่า
- เปิดดูรายละเอียดรายบุคคลได้ เช่น งานที่ส่ง งานที่ยังไม่ส่ง ความก้าวหน้า และ Badge
- สร้างภาพการ์ดผลคะแนนเพื่อแชร์
- มีภาพเคลื่อนไหว/เอฟเฟกต์สำหรับอันดับและพื้นหลัง
- รองรับหน้าจอขนาดต่าง ๆ ด้วย responsive layout

### 4.2 Badge และสถิติส่วนบุคคล

Frontend คำนวณ Badge จากข้อมูลคะแนนและการส่งงาน เช่น ผลงานเด่น ความสม่ำเสมอ หรือความสำเร็จตามเงื่อนไขที่กำหนดในหน้าเว็บ รวมถึงคำนวณ streak จากประวัติการส่งงาน

### 4.3 มินิเกมประจำวัน

- ครูสร้างคลังคำถามแบบ 4 ตัวเลือกได้
- ระบบสุ่มคำถามตามช่วงวันที่ตั้งค่า (`mg_interval_days`)
- คำถามหนึ่งรอบมีผู้ชนะได้หนึ่งคน
- นักเรียนต้องตอบคำถามและกรอกรหัสนักเรียนเพื่อรับคะแนนโบนัส
- ใช้ `LockService` ป้องกันการรับรางวัลพร้อมกันซ้ำซ้อน
- ถ้ามีผู้ตอบผิด คำถามของวันนั้นจะถูก block สำหรับทุกคน
- เมื่อคำถามถูกใช้หมด ระบบสามารถวนกลับมาสุ่มคำถามเดิมได้
- คะแนนรางวัลถูกรวมเข้ากับคะแนนรวมบนกระดาน

## 5. ความสามารถของหน้าครู

หน้าครูมี 6 แท็บหลัก

### 5.1 บันทึกคะแนน

- ดูตารางคะแนนนักเรียนแยกตามห้อง
- ค้นหานักเรียน
- กรอกคะแนนรายงาน/กิจกรรม
- ระบุว่าส่งงานแล้วหรือยัง
- บันทึกหลายช่องพร้อมกัน
- แปลงคะแนนดิบเป็นคะแนนเก็บตามสูตร:

```text
คะแนนเก็บ = round((คะแนนดิบ / คะแนนเต็มดิบ) × คะแนนเก็บเต็ม)
```

- ดูคะแนนรวมและความก้าวหน้าแบบอัปเดตทันทีในหน้าเว็บ
- เปิดรายละเอียดนักเรียนและแก้คะแนนจากหน้ารายบุคคล
- มีการเตือนงานใกล้ครบกำหนด

### 5.2 จัดการนักเรียน

- เพิ่มและแก้ไขข้อมูลนักเรียน
- ฟิลด์หลัก: รหัสนักเรียน คำนำหน้า ชื่อ นามสกุล ห้อง และรูป
- ซ่อนนักเรียนด้วยสถานะ `inactive` โดยไม่ลบข้อมูลจริง
- กู้คืนนักเรียนที่ถูกซ่อน
- อัปโหลดรูปไปยัง Google Drive
- Import รายชื่อนักเรียนจากไฟล์ Excel (`.xlsx`, `.xls`)
- ดาวน์โหลดไฟล์ Excel ตัวอย่างสำหรับ Import
- Export รายชื่อนักเรียนเป็น Excel แยกห้องหรือรวมทุกห้อง

### 5.3 จัดการงาน/กิจกรรม

- เพิ่มและแก้ไขงาน
- กำหนดชื่อ คำอธิบาย คะแนนเต็ม คะแนนเก็บ และวันครบกำหนด
- ผูกงานกับตัวชี้วัด หรือกำหนดเป็นคะแนนสอบ
- ซ่อน/แสดงงานโดยไม่ลบคะแนนเดิม
- กู้คืนงานที่ถูกซ่อน

### 5.4 จัดการตัวชี้วัด

- เพิ่ม แก้ไข และจัดลำดับตัวชี้วัด
- ผูกคะแนนจากงานเข้ากับตัวชี้วัด
- ไม่อนุญาตให้ลบตัวชี้วัดที่ยังมีงาน active ผูกอยู่
- Export คะแนนแยกตามตัวชี้วัด คะแนนสอบ คะแนนมินิเกม และคะแนนรวม

### 5.5 รายงานและสถิติ

- สรุปจำนวนนักเรียน งาน การส่งงาน และความก้าวหน้า
- กรองข้อมูลตามห้องเรียน
- แสดงกราฟคะแนน/การส่งงานด้วย Chart.js
- แสดง Heatmap การส่งงาน
- แสดงกลุ่มนักเรียนที่ควรติดตาม
- คัดลอกรายชื่อสำหรับนำไปใช้แจ้งเตือนผ่าน LINE
- Export ตารางสถิติเป็น Excel
- Export กราฟเป็นรูปภาพ
- Export รายงานรายห้องเป็น Excel หลายชีต
- สร้างรายงาน PDF รายห้องผ่านหน้าพิมพ์ของ Browser
- เลือกนักเรียนหลายคนและสร้างรายงาน PDF รายบุคคลรวมในไฟล์เดียว

### 5.6 จัดการมินิเกม

- เพิ่มและแก้ไขคำถาม ตัวเลือก คำตอบ และคะแนนโบนัส
- ซ่อน/แสดง/ลบคำถาม
- ดูสถานะคำถามประจำวัน
- เปิดโอกาสให้เล่นใหม่หลังคำถามถูก block
- รีเซ็ตคำถามให้กลับเข้าสู่รอบสุ่ม
- ดูประวัติผู้ได้รับ Badge/คะแนนโบนัส

### 5.7 ตั้งค่า

- ชื่อโรงเรียนหรือชื่อที่แสดงบนระบบ (`school_name`)
- รหัสผ่านครู (`teacher_password`)
- เปิด/ปิดมินิเกม (`minigame_enabled`)
- กำหนดช่วงวันของมินิเกม (`mg_interval_days`)
- เปิด/ปิดแท็บรายงาน (`report_enabled`)

ค่าเริ่มต้นที่ backend สร้างให้อัตโนมัติคือ เปิดมินิเกม, เล่นได้ทุก 1 วัน และเปิดรายงาน

## 6. โครงสร้างข้อมูลใน Google Sheets

ระบบใช้ชื่อชีตและคอลัมน์ดังนี้ โดยแถวแรกต้องเป็น Header

### `settings`

| คอลัมน์ | ความหมาย |
|---|---|
| `key` | ชื่อการตั้งค่า |
| `value` | ค่าการตั้งค่า |

### `students`

| คอลัมน์ | ความหมาย |
|---|---|
| `id` | รหัสภายใน |
| `student_code` | รหัสนักเรียน |
| `title` | คำนำหน้า |
| `firstname` | ชื่อ |
| `lastname` | นามสกุล |
| `class` | ห้องเรียน |
| `photo_url` | URL รูปนักเรียน |
| `status` | `active` หรือ `inactive` |

### `assignments`

| คอลัมน์ | ความหมาย |
|---|---|
| `id` | รหัสงาน |
| `title` | ชื่องาน |
| `description` | คำอธิบาย |
| `max_score` | คะแนนเต็มดิบ |
| `collect_score` | คะแนนเก็บเต็ม |
| `due_date` | วันครบกำหนด |
| `status` | `active` หรือ `inactive` |
| `indicator_id` | ตัวชี้วัดที่ผูกกับงาน |
| `is_exam` | ระบุว่าเป็นคะแนนสอบ |

### `scores`

| คอลัมน์ | ความหมาย |
|---|---|
| `id` | รหัสรายการคะแนน |
| `student_id` | อ้างอิง `students.id` |
| `assignment_id` | อ้างอิง `assignments.id` |
| `score` | คะแนนดิบ |
| `submitted` | สถานะส่งงาน โดยโค้ดตรวจค่า `yes` |
| `update_time` | วันที่อัปเดต รูปแบบ `yyyy-MM-dd` |

### `indicators`

| คอลัมน์ | ความหมาย |
|---|---|
| `id` | รหัสตัวชี้วัด |
| `title` | ชื่อตัวชี้วัด |
| `description` | คำอธิบาย |
| `order` | ลำดับแสดงผล |
| `status` | สถานะ |

### `minigame`

| คอลัมน์ | ความหมาย |
|---|---|
| `id` | รหัสคำถาม |
| `question` | คำถาม |
| `choice_a` ถึง `choice_d` | ตัวเลือก 4 ข้อ |
| `answer` | คำตอบที่ถูก |
| `bonus_score` | คะแนนโบนัส |
| `status` | สถานะคำถาม |
| `show_date` | วันที่นำคำถามมาแสดง |
| `claimed_by` | รหัสนักเรียนผู้ตอบถูก |
| `claimed_at` | เวลารับรางวัล |
| `blocked_at` | เวลาที่คำถามถูก block จากการตอบผิด |

### `minigame_rewards`

| คอลัมน์ | ความหมาย |
|---|---|
| `id` | รหัสรายการรางวัล |
| `student_id` | อ้างอิง `students.id` |
| `question_id` | อ้างอิง `minigame.id` |
| `score` | คะแนนโบนัส |
| `claimed_at` | เวลาที่ได้รับรางวัล |

## 7. การจัดเก็บไฟล์รูป

เมื่ออัปโหลดรูป ระบบทำงานดังนี้:

1. ใช้ชื่อ Spreadsheet เป็นชื่อโฟลเดอร์หลัก
2. ย้ายไฟล์ Spreadsheet เข้าโฟลเดอร์ดังกล่าวถ้ายังไม่ได้อยู่ในนั้น
3. สร้างโฟลเดอร์ย่อย `student_photos`
4. อัปโหลดรูปนักเรียนเข้าโฟลเดอร์นี้
5. ตั้งสิทธิ์โฟลเดอร์และไฟล์เป็น “Anyone with the link — View”
6. บันทึก URL รูปแบบ `https://lh3.googleusercontent.com/d/FILE_ID`

## 8. ลำดับการทำงานของข้อมูล

```text
ผู้ใช้เปิด Web App
        |
        v
doGet() เลือก index หรือ teacher
        |
        v
HTML เรียก backend ผ่าน google.script.run
        |
        v
code.gs อ่าน/เขียน Google Sheets และ Google Drive
        |
        v
ส่ง Object/Array กลับไป render ใน Browser
```

หน้าแรกเรียก `getAllData()` เพื่อรวมข้อมูลนักเรียน งาน คะแนน การตั้งค่า และคะแนนโบนัส แล้วคำนวณอันดับบน server

หน้าครูเรียก `getTeacherDataFast()` ก่อนเพื่อให้แท็บคะแนนโหลดเร็ว จากนั้นจึง lazy-load ข้อมูลมินิเกมเมื่อจำเป็น ส่วนรายงานสามารถเรียก `getTeacherData()` เพื่อโหลดข้อมูลครบชุด

## 9. ข้อสังเกตและความเสี่ยงจากการตรวจโค้ด

### ระดับวิกฤต: การยืนยันตัวตนไม่ได้ป้องกัน Backend

รหัสผ่านครูถูกตรวจด้วย `checkTeacherPassword()` เพื่อเปิดหน้า UI เท่านั้น แต่ฟังก์ชันแก้ไขข้อมูลฝั่ง server เช่น `addStudent`, `saveAllScores`, `updateSetting`, `deleteAssignment` และฟังก์ชันจัดการมินิเกม **ไม่ได้ตรวจ session/token หรือรหัสผ่านซ้ำ**

ผลคือ หาก Web App เปิดให้คนทั่วไปเข้าถึง ผู้ที่เข้าถึงหน้า Apps Script และเรียก `google.script.run` ได้อาจเรียกฟังก์ชันแก้ไขข้อมูลโดยตรง แม้ไม่ได้ผ่านหน้าล็อกอินตามปกติ

ข้อเสนอแนะ: ใช้ session token อายุสั้นที่สร้างหลังยืนยันรหัส และให้ทุก mutation ตรวจ token ฝั่ง server หรือจำกัดสิทธิ์ Deploy Web App ให้เฉพาะบัญชีที่อนุญาต

### ระดับสูง: รหัสผ่านถูกส่งผ่าน URL

หน้าแรกเปลี่ยนไปหน้าครูด้วย `?page=teacher&pw=...` ทำให้รหัสผ่านอาจปรากฏใน browser history, URL ที่แชร์, log และ referrer

ข้อเสนอแนะ: ไม่ส่งรหัสผ่านใน URL ใช้ token แบบครั้งเดียว/อายุสั้นหรือกระบวนการล็อกอินภายในหน้าครูแทน

### ระดับสูง: เก็บรหัสผ่านเป็นข้อความธรรมดา

รหัสผ่านครูถูกเก็บในชีต `settings` และเปรียบเทียบแบบข้อความตรง ๆ ไม่มี hashing, salting, rate limiting หรือการล็อกเมื่อเดารหัสผิดหลายครั้ง

ข้อเสนอแนะ: ใช้ Google account identity/สิทธิ์ Workspace เป็นหลัก หรือเก็บ salted password hash ใน Script Properties และเพิ่ม rate limiting

### ระดับสูง: ข้อมูลนักเรียนและรูปอาจเปิดเผยต่อสาธารณะ

หน้าหลักแสดงชื่อ ห้อง รูป คะแนน อันดับ และสถานะการส่งงาน และโค้ดตั้งสิทธิ์โฟลเดอร์/รูปเป็น Anyone with the link หาก URL ของ Web App หรือรูปถูกเผยแพร่ ข้อมูลส่วนบุคคลของนักเรียนอาจถูกเข้าถึงได้

ข้อเสนอแนะ: ทบทวนฐานกฎหมาย/ความยินยอม นโยบายโรงเรียน และจำกัดข้อมูลที่แสดงต่อสาธารณะ เช่น ใช้นามแฝง ปิดรูป หรือจำกัดผู้เข้าถึง

### ระดับสูง: เสี่ยง Stored XSS

หลายจุดสร้าง UI ด้วย `innerHTML` หรือ template string โดยนำค่าจาก Sheets เช่น ชื่อ งาน ห้อง และข้อความอื่นมาแทรกโดยไม่พบฟังก์ชัน escape HTML กลาง หากผู้ใช้ใส่ HTML/JavaScript ลงในข้อมูล อาจทำให้ script ทำงานใน Browser ได้

ข้อเสนอแนะ: ใช้ `textContent` สำหรับข้อความทั่วไป หรือ escape ค่า HTML และตรวจ/จำกัด URL รูปก่อน render

### ระดับกลาง: การตั้งค่า iframe ผ่อนคลาย

ทั้งสองหน้าใช้ `XFrameOptionsMode.ALLOWALL` จึงสามารถถูกฝังในเว็บไซต์อื่นได้ และเพิ่มความเสี่ยง clickjacking โดยเฉพาะหน้าครู

ข้อเสนอแนะ: ยกเลิก `ALLOWALL` หากไม่จำเป็น หรือเพิ่มมาตรการป้องกันการฝังและยืนยันการกระทำสำคัญ

### ระดับกลาง: การบันทึกหลายคะแนนไม่เป็นธุรกรรมเดียว

`saveAllScores()` วนเรียก `saveScore()` ทีละรายการ ไม่มี batch write, transaction หรือการรวมผลล้มเหลว หากบางรายการผิดพลาด ระบบยังคืน `{ success: true }` ได้ และข้อมูลอาจบันทึกสำเร็จเพียงบางส่วน

ข้อเสนอแนะ: ตรวจข้อมูลทั้งหมดก่อนเขียน ใช้ batch ranges/LockService และคืนผลสำเร็จหรือข้อผิดพลาดรายรายการ

### ระดับกลาง: การสร้าง ID มีโอกาสชนกัน

หลายฟังก์ชันใช้ `getNextId()` ซึ่งอ่านค่าสูงสุดแล้วบวก 1 โดยไม่มี lock (ยกเว้น flow รับรางวัลมินิเกม) หากมีการเพิ่มข้อมูลพร้อมกัน อาจได้ ID ซ้ำ

ข้อเสนอแนะ: ครอบการสร้าง ID และ append ด้วย `LockService` หรือใช้ UUID

### ระดับกลาง: การลบ/แก้ข้อมูลสัมพันธ์ยังไม่ครบ

- การซ่อนนักเรียนและงานเก็บข้อมูลเดิมไว้ ซึ่งเหมาะกับประวัติ แต่ควรกำหนดนโยบายให้ชัดเจน
- การลบคำถามมินิเกมเป็นการลบแถวจริง แม้อาจมี `minigame_rewards.question_id` อ้างอิงอยู่
- ไม่พบ foreign-key validation สำหรับ `student_id`, `assignment_id` และ `indicator_id`

### ระดับกลาง: การตรวจสอบข้อมูลนำเข้ายังจำกัด

Backend ของ `importStudents()` เพิ่มข้อมูลทุกแถวโดยตรง ไม่ตรวจรหัสนักเรียนซ้ำ ความยาวข้อความ รูปแบบห้อง หรือจำนวนรายการสูงสุด และไม่มี transaction

### ระดับต่ำถึงกลาง: การพึ่งพา CDN

Chart.js, `xlsx-js-style` และ Google Fonts โหลดจากภายนอก หาก CDN ใช้งานไม่ได้ ฟีเจอร์กราฟ/Excel/ฟอนต์อาจไม่ทำงาน และไม่มี Subresource Integrity (SRI)

### ระดับต่ำ: ความสามารถบำรุงรักษา

`index.html` และโดยเฉพาะ `teacher.html` รวม markup, style และ logic จำนวนมากไว้ในไฟล์เดียว ทำให้ทดสอบ แก้ไข และ review ยากขึ้น อีกทั้งไม่พบ automated tests หรือ deployment configuration ในโฟลเดอร์นี้

## 10. สิ่งที่ตรวจสอบได้และข้อจำกัด

การตรวจครั้งนี้ยืนยันจาก source code ว่ามีฟังก์ชันและ flow ตามที่อธิบาย แต่ยังไม่ได้ทดสอบกับ Spreadsheet/Drive จริงหรือ deployment จริง เพราะในโฟลเดอร์ไม่มี:

- Spreadsheet ตัวอย่างและข้อมูลจริง
- `appsscript.json` หรือข้อมูล deployment
- credential/สิทธิ์ Google Drive
- test suite
- URL Web App ที่กำลังใช้งาน

ดังนั้นพฤติกรรมที่ขึ้นกับสิทธิ์ Deploy, รูปแบบข้อมูลจริง, timezone และข้อจำกัด quota ของ Google Apps Script ควรทดสอบบน environment ที่ใช้งานจริงเพิ่มเติม

## 11. ลำดับปรับปรุงที่แนะนำ

1. ป้องกันฟังก์ชันแก้ไขข้อมูลทุกตัวด้วย server-side authorization
2. เลิกส่งรหัสผ่านผ่าน URL และย้ายออกจาก Google Sheet
3. ป้องกัน Stored XSS ด้วย output escaping/safe DOM rendering
4. ทบทวนการเปิดเผยข้อมูลและรูปนักเรียน
5. ทำ batch write, validation และ locking สำหรับการบันทึกคะแนน/นำเข้าข้อมูล
6. เพิ่ม `appsscript.json`, เอกสาร Deploy, ชุดข้อมูลตัวอย่าง และ automated tests
7. แยก JavaScript/CSS ของหน้าครูเป็นโมดูลย่อยเพื่อลดความซับซ้อน

